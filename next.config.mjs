/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['images.thdstatic.com'], // Agrega aquí los dominios de las imágenes permitidos
    },
  };
  
  export default nextConfig;
  